import React from 'react';
import useTitle from '../../Hooks/useTitle';

const Blog = () => {

    useTitle("Cuddle Corner | Blog")

    return (
        <div>
            
        </div>
    );
};

export default Blog;